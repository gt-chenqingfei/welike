<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="38dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/search_sug_btn"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:scaleType="center"
        android:paddingLeft="15dp"
        android:paddingRight="15dp"
        app:srcCompat="@drawable/search_sug_arrow_icon" />

    <TextView
        android:id="@+id/search_sug_item"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginLeft="15dp"
        android:layout_toLeftOf="@id/search_sug_btn"
        android:drawableLeft="@drawable/search_sug_icon"
        android:layout_alignParentLeft="true"
        android:drawablePadding="5dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:gravity="center_vertical"
        android:textColor="@color/search_sug_text_color"
        android:textSize="@dimen/search_sug_text_size" />


    <View
        android:layout_width="match_parent"
        android:layout_height="@dimen/common_divider_px1"
        android:layout_alignParentBottom="true"
        android:layout_marginLeft="36dp"
        android:background="@color/common_search_item_divider" />
</RelativeLayout>